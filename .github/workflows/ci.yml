name: ci

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: windows-latest
    timeout-minutes: 20
    env:
      GOARCH: amd64
      CGO_ENABLED: 0
      GOTOOLCHAIN: local
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci
      - name: Check generated-binding boundary
        shell: pwsh
        run: |
          $imports = Get-ChildItem -Path frontend/src -Recurse -File -Include "*.ts", "*.tsx" |
            Select-String -Pattern 'from\s+["''][^"'']*wailsjs/'
          if ($imports) { $imports; throw "Frontend source imports ignored Wails generated files." }
      - name: Check formatting
        shell: pwsh
        run: |
          $goFiles = Get-ChildItem -Path . -Recurse -Filter "*.go" -File
          $files = $goFiles | ForEach-Object { gofmt -l $_.FullName }
          if ($files) { $files; exit 1 }
      - name: Frontend type check
        working-directory: frontend
        run: npm run typecheck
      - name: Frontend build
        working-directory: frontend
        run: npm run build
      - name: Vet
        run: go vet ./...
      - name: Test
        run: go test ./... -count=1
